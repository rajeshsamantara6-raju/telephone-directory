let staffData = []; async function loadDirectory(){ const response = await fetch("staff.json"); staffData = await response.json(); renderDirectory(staffData); } function renderDirectory(data){ const container = document.getElementById("directory"); container.innerHTML = ""; const departments = {}; data.forEach(item=>{ if(!departments[item.department]){ departments[item.department]=[]; } departments[item.department].push(item); }); Object.keys(departments).sort().forEach(dept=>{ const staff = departments[dept]; let html = ` <div class="department-card"> <div class="department-header"> <span>${dept}</span> <span>${staff.length} staff</span> </div> <table> <thead> <tr> <th>Name</th> <th>Designation</th> <th>Ext</th> <th>Email</th> <th>Location</th> </tr> </thead> <tbody> `; staff.forEach(s=>{ html += ` <tr> <td class="name">${s.name}</td> <td>${s.designation}</td> <td>${s.ext}</td> <td><a class="email" href="mailto:${s.email}">${s.email}</a></td> <td>${s.location}</td> </tr> `; }); html += ` </tbody> </table> </div> `; container.innerHTML += html; }); } function searchStaff(){ const q = document .getElementById("searchInput") .value .toLowerCase(); const filtered = staffData.filter(s=> s.name.toLowerCase().includes(q) || s.department.toLowerCase().includes(q) || s.designation.toLowerCase().includes(q) || String(s.ext).includes(q) || s.email.toLowerCase().includes(q) || s.location.toLowerCase().includes(q) ); renderDirectory(filtered); } loadDirectory();
